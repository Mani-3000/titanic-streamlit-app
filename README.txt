Steps to run:

1. Ensure you have Streamlit installed:
   pip install streamlit

2. (Optional) If you don't have the Titanic dataset, download it and place it as "titanic.csv" in this folder.

3. Run preprocessing and model training:
   python preprocessing_and_model.py

4. Run the Streamlit app:
   streamlit run app.py
